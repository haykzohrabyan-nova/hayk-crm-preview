/** List navigation: show cache first, then background sync (not realtime). */
export const LIST_NAV_REVALIDATE_MS = 300;

/** Realtime / window events: no intentional delay before refetch. */
export const REALTIME_REFETCH_MS = 0;
