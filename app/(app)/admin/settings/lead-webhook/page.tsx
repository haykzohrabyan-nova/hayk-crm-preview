import { LeadWebhookSection } from "@/components/admin/lead-webhook-section";

export const metadata = { title: "Lead Webhook — Admin" };

export default function LeadWebhookPage() {
  return <LeadWebhookSection />;
}
