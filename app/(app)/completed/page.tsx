import { CompletedPage } from "@/components/orders/completed-page";

export const metadata = { title: "Completed — BazaarPrinting CRM" };

export default function Page() {
  return <CompletedPage />;
}
